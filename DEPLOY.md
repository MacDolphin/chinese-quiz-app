# 部署與更新詳細指南 (Deployment & Update Guide)

這份指南將一步步教您如何將電腦上的程式碼部署到伺服器，以及日後如何更新。

## 📋 前置準備

1.  **本地端 (您的電腦)**: 已經安裝 Git，並且程式碼已經連結到 GitHub 倉庫。
    *   倉庫網址: `https://github.com/MacDolphin/chinese-quiz-app.git`
2.  **伺服器端**: 已經可以透過 SSH 連線，並且安裝了 Python 和 Git。

---

## 🚀 情況一：首次部署 (第一次把程式放到伺服器)

如果您是第一次在伺服器上執行此專案，請依照以下步驟：

### 1. 連線到伺服器
開啟終端機，連線到您的伺服器 (假設 IP 是 `your-server-ip`)：
```bash
ssh username@your-server-ip
```

### 2. 下載程式碼 (Clone)
在伺服器上，將程式碼從 GitHub 下載下來：
```bash
# 建議先進入您想放專案的資料夾，例如家目錄
cd ~

# 下載專案
git clone https://github.com/MacDolphin/chinese-quiz-app.git

# 進入專案資料夾
cd chinese-quiz-app
```

### 3. 安裝依賴套件
安裝專案所需的 Python 套件：
```bash
pip install -r requirements.txt
```

### 4. 啟動應用程式
使用 `nohup` 讓程式在背景執行，這樣即使您斷開連線，程式也會繼續跑：
```bash
nohup streamlit run quiz_app_gui.py --server.port 8501 &
```
*   現在，您應該可以透過 `http://伺服器IP:8501` 看到網頁了。

---

## 🔄 情況二：日常更新 (當您修改了程式碼)

當您在電腦上修改了程式碼 (例如改了生字、修了 Bug)，想要更新到伺服器時：

### 步驟 1: 在本地端 (您的電腦)

#### 選項 A: 使用終端機 (Terminal)
1.  **開啟終端機**，進入專案資料夾：
    ```bash
    cd "/Users/jackallee/Test Folder/quiz_gui"
    ```
2.  **提交修改**：
    ```bash
    git add .
    git commit -m "修改說明"
    git push
    ```

#### 選項 B: 使用 GitHub Desktop (推薦 👍)
如果您有安裝 GitHub Desktop，這會更簡單：
1.  打開 **GitHub Desktop**。
2.  確認左上角選擇的是 `chinese-quiz-app` 專案。
    *   如果還沒加入，請點選 `File` -> `Add Local Repository` -> 選擇 `/Users/jackallee/Test Folder/quiz_gui` 資料夾。
3.  在左側會看到您修改的檔案。
4.  在左下角的 **Summary** 欄位輸入修改說明 (例如 "修改生字")。
5.  點擊藍色的 **Commit to main** 按鈕。
6.  點擊右上角的 **Push origin** 按鈕。
    *   這樣就成功上傳到 GitHub 了！

### 步驟 2: 在伺服器端 下載並重啟

1.  **連線到伺服器**：
    ```bash
    ssh username@your-server-ip
    ```

2.  **進入專案資料夾並更新**：
    ```bash
    cd ~/chinese-quiz-app  # (請確認這是您存放專案的路徑)
    git pull
    ```
    *   這行指令會把 GitHub 上最新的程式碼抓下來。

3.  **重新啟動應用程式**：
    為了讓新程式碼生效，通常需要重啟 Streamlit。

    *   **方法 A (簡單版)**: 如果您只是改了 `vocabulary.csv` 或簡單的 Python 檔，Streamlit 網頁右上角通常會出現 "Rerun" 或 "The app has changed"，直接點擊即可。
    
    *   **方法 B (完整重啟)**: 如果網頁沒反應，或您改了設定檔，請執行：
        ```bash
        # 1. 找到並殺死舊的程序
        pkill -f "streamlit run quiz_app_gui.py"

        # 2. 重新啟動
        nohup streamlit run quiz_app_gui.py --server.port 8501 &
        ```

---

## 🛠 常見問題排除

*   **Git Push 失敗？**
    *   如果出現 `Permission denied`，請確認您有輸入正確的 GitHub 帳號密碼 (或 Personal Access Token)。
*   **伺服器上 `git pull` 失敗？**
    *   如果出現 `Your local changes would be overwritten by merge`，表示您在伺服器上也改了檔案。請先決定要保留哪邊的修改，或執行 `git stash` 先暫存伺服器的修改。
